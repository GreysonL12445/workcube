#include <PRIZM.h> ;
PRIZM blyat ;

int counter = 0 ;

void setup() {
  Serial.begin(9600);
  Serial.println("start of Setup");
   delay(1000);

}

void loop() {
  counter +=1 ;
  Serial.println("Start of Loop" + String(conter));
  Serial.println("end of Loop" + String(counter));
  delay(1000);
}
